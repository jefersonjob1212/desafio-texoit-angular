import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AbstractListTableComponent } from './abstract-list-table.component';

describe('AbstractListTableComponent', () => {
  // let component: AbstractListTableComponent<any>;
  // let fixture: ComponentFixture<AbstractListTableComponent<any>>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [AbstractListTableComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(AbstractListTableComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });
});
